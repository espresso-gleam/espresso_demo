{
  "name": "espresso_demo",
  "scripts": {
    "dev": "npx concurrently npm:dev:app npm:dev:templatizer",
    "dev:app": "npx nodemon -e gleam --exec gleam run",
    "dev:templatizer": "node ../espresso_templates/src/cli.js watch"
  }
}
